apiVersion: v1
kind: Secret
metadata:
  name: scanpro-external-db-secret
  namespace: scanpro
type: Opaque
data:
  # Note: These values are base64 encoded
  # You can encode your values with: echo -n "your-value" | base64
  POSTGRES_USER: "c2NhbnBybw==" # "scanpro" in base64
  POSTGRES_PASSWORD: "QXFzd2RlITEyMw==" # Replace with your actual password in base64
  POSTGRES_DB: "c2NhbnBybw==" # "scanpro" in base64
  REDIS_PASSWORD: "" # Add your Redis password in base64 if required
